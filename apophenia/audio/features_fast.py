"""Per-channel fast features: RMS, spectral centroid, onset.

Stub for phase 2. Will run inside `audio_proc` at ~100Hz on each block from
`AudioSource.frames()`. Outputs OSC messages on `/audio/fast/...`.
"""
